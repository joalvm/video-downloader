<!DOCTYPE html>
<html lang="es" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Downloader</title>
    <script nonce="<%= nonce %>" type="text/javascript" charset="utf-8" src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-full flex items-center justify-center bg-gradient-to-b from-gray-50 to-gray-100">
    <div class="w-full max-w-2xl mx-auto p-6">
        <div class="relative">
            <input type="text" id="urlInput" placeholder="Pega la URL del video aquí" class="w-full px-4 py-3 pr-24 rounded-full border border-gray-300 focus:border-gray-400 focus:outline-none shadow-md transition-all duration-300">
            <div class="absolute right-2 top-1/2 transform -translate-y-1/2 flex">
                <button id="pasteBtn" class="p-2 text-gray-500 hover:text-gray-700 transition-colors duration-200">
                    <i data-lucide="clipboard" class="h-5 w-5"></i>
                </button>
                <button id="searchBtn" class="p-2 text-blue-500 hover:text-blue-700 transition-colors duration-200">
                    <i data-lucide="search" class="h-5 w-5"></i>
                </button>
            </div>
        </div>
        <div id="videoInfo" class="mt-8 p-6">
        </div>
    </div>

    <script nonce="<%= nonce %>" type="text/javascript" charset="utf-8" src="https://unpkg.com/lucide@latest"></script>
    <script nonce="<%= nonce %>" type="text/javascript" charset="utf-8">
        lucide.createIcons();
    </script>
    <script nonce="<%= nonce %>" type="text/javascript"charset="utf-8" src="static/js/index.js"></script>
</body>
</html>
